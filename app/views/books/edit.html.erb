<div class="edit">

	<h2>Editing Book</h2>

	<%= form_for(@book) do |f| %>

		<h4>Title</h4>
		<%= f.text_field :tite, class: "book_title" %>

		<h4>Opinion</h4>
		<%= f.text_area :body, class: "book_body" %>
		<br><%= f.submit 'Update Book' %></br>

	<% end %>

</div>